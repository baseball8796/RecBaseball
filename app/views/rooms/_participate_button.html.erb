<%= form_with(model: current_user.rooms.build, local: true) do |f| %>
      <%= hidden_field_tag :participate_id, user.id %>
      <%= f.submit 'チャットを始める', class: 'btn btn-primary btn-block' %>
<% end %>
